# Instagram Auto Uploader - Chrome Extension

Chrome MCP를 활용한 Instagram 자동 이미지 업로드 확장 프로그램

## 📋 주요 기능

### 1. 수동 업로드 모드
- Instagram에서 "만들기" 버튼을 먼저 클릭
- 모달이 열리면 확장 프로그램에서 이미지 URL 입력
- "수동 업로드" 버튼 클릭하여 이미지 주입

### 2. 자동 업로드 모드 (MCP 스타일)
- Instagram 메인 페이지에서 확장 프로그램 실행
- 이미지 URL 입력 후 "자동 업로드" 버튼 클릭
- 자동으로 "만들기" 버튼을 찾아 클릭하고 이미지 주입

### 3. 디버그 모드
- 현재 페이지의 파일 입력 요소들을 콘솔에 출력
- 문제 해결 시 유용

## 🚀 설치 방법

1. Chrome 브라우저에서 `chrome://extensions/` 접속
2. 우측 상단 "개발자 모드" 활성화
3. "압축해제된 확장 프로그램을 로드합니다" 클릭
4. `chrome_extension` 폴더 선택

## 💡 사용 방법

### 방법 1: 수동 업로드
```
1. Instagram (https://www.instagram.com) 접속
2. 좌측 메뉴에서 "만들기" 버튼 클릭
3. 모달이 열리면 확장 프로그램 아이콘 클릭
4. 이미지 URL 입력
5. "🎯 수동 업로드" 버튼 클릭
```

### 방법 2: 자동 업로드 (권장)
```
1. Instagram (https://www.instagram.com) 메인 페이지 접속
2. 확장 프로그램 아이콘 클릭
3. 이미지 URL 입력
4. "🚀 자동 업로드" 버튼 클릭
5. 자동으로 모달이 열리고 이미지가 업로드됨
```

## 🔧 기술 스택

### Core Files

#### 1. `instagram-uploader.js`
Instagram의 DOM 구조를 분석하고 이미지를 자동으로 주입하는 핵심 클래스

**주요 메서드:**
- `findCreatePostButton()`: 새 게시물 버튼 자동 탐지
- `findFileInputInModal()`: 모달 내부의 파일 입력 찾기
- `injectImage()`: URL에서 이미지를 fetch하여 File 객체로 변환 후 주입
- `uploadImage()`: 전체 업로드 프로세스 자동화

#### 2. `mcp-automation.js`
Chrome MCP 스타일의 자동화 스크립트 (고급 사용자용)

**주요 기능:**
- 페이지 스냅샷 분석
- 요소 UID 기반 탐색
- JavaScript 코드 생성기
- 전체 자동화 워크플로우

#### 3. `content.js`
Chrome Extension의 Content Script

**역할:**
- 백그라운드 스크립트와 통신
- Instagram 페이지에 스크립트 주입
- 메시지 기반 이벤트 처리

#### 4. `popup.html` & `popup.js`
확장 프로그램 UI

## 📊 작동 원리

### 이미지 주입 프로세스

```javascript
1. 이미지 URL fetch
   ↓
2. Blob 생성
   ↓
3. File 객체 생성
   ↓
4. DataTransfer로 파일 할당
   ↓
5. React 이벤트 발생 (input, change)
   ↓
6. Instagram이 파일 인식
```

### DOM 탐색 전략

```javascript
// 모달 우선 확인
const modal = document.querySelector('[role="dialog"]');

// 모달 내부에서만 input 검색
const inputs = modal.querySelectorAll('input[type="file"]');

// accept 속성으로 이미지 입력 필터링
const imageInput = [...inputs].find(i => i.accept.includes('image'));
```

## 🔍 Chrome MCP 활용

이 확장 프로그램은 Chrome MCP의 다음 개념을 활용합니다:

### 1. 요소 탐색 (Element Discovery)
- `aria-label` 속성 활용
- `role` 속성으로 모달/버튼 식별
- 텍스트 내용 기반 검색

### 2. 이벤트 시뮬레이션
- React 호환 이벤트 발생
- `bubbles`, `composed` 옵션 활용
- DataTransfer API 사용

### 3. 비동기 대기
- Promise 기반 폴링
- setTimeout을 활용한 재시도
- 타임아웃 처리

## 🐛 문제 해결

### "Input not found" 오류
```javascript
// 콘솔에서 실행:
window.debugFileInputs();

// 모달이 열려있는지 확인:
document.querySelector('[role="dialog"]')
```

### CORS 오류
- 이미지 URL이 CORS를 허용하는지 확인
- 필요시 프록시 서버 사용

### React 이벤트 미인식
```javascript
// input 요소에 직접 접근하여 확인:
const input = document.querySelector('input[type="file"]');
console.log(input.files); // FileList 확인
```

## 📝 개발자 노트

### MCP 스냅샷 예시
```
uid=1 link "홈" role=link
uid=2 button "검색" role=button
uid=7 link "만들기" role=link aria-label="새로 만들기"
uid=13 input type=file accept="image/jpeg,image/png"
```

### JavaScript Injection Script
```javascript
// evaluate_script를 사용한 직접 주입
const script = automation.generateInjectionScript(imageUrl);
// chromeMCP.evaluateScript(script);
```

## 🎯 향후 계획

- [ ] 드래그 앤 드롭 시뮬레이션
- [ ] 멀티 이미지 업로드
- [ ] 캡션 자동 입력
- [ ] 해시태그 자동 추가
- [ ] 스케줄 업로드
- [ ] 로컬 파일 업로드 지원

## 📄 라이센스

MIT License

## 🤝 기여

Pull Request 환영합니다!

## ⚠️ 주의사항

- Instagram의 이용약관을 준수하세요
- 과도한 자동화는 계정 제한을 초래할 수 있습니다
- 이 도구는 교육 목적으로만 사용하세요
